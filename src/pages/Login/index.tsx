import Button from "../ ../components/Button";
import Input from "../ ../components/Input";

import { Container, LoginContainer, Column, Spacing, Title }

const Login = () => {
    return (
        <Container>
        <LoginContainer>

        </LoginContainer>
        <Container/>
    );
};

export default Login;